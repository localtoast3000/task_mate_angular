import { SystemThemeDirective } from './system-theme.directive';

describe('SystemThemeDirective', () => {
  it('should create an instance', () => {
    const directive = new SystemThemeDirective();
    expect(directive).toBeTruthy();
  });
});
